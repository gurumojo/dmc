version: "2"
services:
  environment:
    environment:
      GURUMOJO_ENVIRONMENT: development
      TERM: linux
    networks:
      - gurumojo
  base:
    extends:
      service: environment
    volumes:
      - etc:/opt/gurumojo/etc:rw
      - lib:/opt/gurumojo/lib:rw
