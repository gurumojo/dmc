version: "2"
services:
  environment:
    environment:
      TERM: linux
    networks:
      - gurumojo
  base:
    extends:
      service: environment
    volumes:
      - etc:/opt/gurumojo/etc
      - lib:/opt/gurumojo/lib
      #- var:/opt/gurumojo/var
